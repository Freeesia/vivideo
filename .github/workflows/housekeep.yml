name: HouseKeep

on:
  schedule:
    - cron: '0 0 * * 6'
  push:
jobs:
  dotnet:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-dotnet@v1.4.0
    - run: |
        dotnet tool install nukeeper --global
        dotnet nukeeper repo https://github.com/Freeesia/vivideo ${{ secrets.GitHubToken }}
